/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     formats: ['image/avif', 'image/webp'],
    // }, // avif take 20% more time to compress but makes 20% less in size than webp
};

module.exports = nextConfig;
